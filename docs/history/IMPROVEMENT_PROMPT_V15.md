# Task: V15 — the provable-change refactor: linear pipeline, dead code out, strict lints in, bugs FIXED not frozen

You are working in `/Users/kisaczka/Desktop/code/slovowiki` (master, post-PR #114, `data-v1` tagged). This brief was produced by a 13-agent audit (8 subsystem auditors, a clippy census, 3 pipeline researchers, a completeness critic); every finding below carries file:line evidence from that audit — verify, don't rediscover.

**The discipline is NO UNDECLARED BEHAVIOR CHANGE — not no behavior change.** Two commit tracks:

- **Track A (refactor/motion commits)** prove byte-identity: the export tree hash unchanged, benchmarks pinned (probe 147/44/28, evaluate 42.02%, aspect 17.88/48.71/89.07), tests green. CI makes this nearly free (the double-export sha256 at ci.yml:45-48 plus the artifact git-diff guards) — exploit it: motion is only trusted because it is proven inert.
- **Track B (behavior-repair commits)** fix bugs the audit surfaced, each as its OWN commit with: the wrong behavior demonstrated (test or transcript) BEFORE, the right behavior pinned AFTER, and a tree diff scoped to exactly the declared change. Buggy behavior is never preserved for the harness's convenience — the harness exists so repairs are legible, not so defects are immortal.
- Standing rule: a bug **discovered mid-refactor** never rides a Track A commit. It becomes its own Track B commit, or a logged entry for the next brief — silently normalizing it and silently preserving it are both failures.

Scope fence: behavior IMPROVEMENTS beyond the audit's enumerated bugs (new checks, new features, semantic unification of the gloss tokenizers) stay out — this brief repairs and restructures; it does not extend. And the full site decomposition (export_corpus's 1,808-line god function, navigation.rs's six concerns, english_api.rs's three fused programs, coverage.rs's 170-line duplicate of the export pipeline) is **deferred to V16** — the audit sized it L and the critic's risk-per-effort sequence ranks everything below above it. V15 ships the mechanical wave, the boundary fixes, the lints, and the pipeline infrastructure; V16 then decomposes the site against a much safer baseline. One commit per numbered item.

## 1. Provable-dead deletions (audit-verified, batch)

Delete, citing the audit evidence per hunk: `ConsensusConfig::full()` (consensus.rs:229, zero callers); the duplicate `("voz","vȯz")` PREFIX_VARIANTS row (proto_link.rs:343); `Pos::parse`'s repeated `starts_with("m.")` disjunct (model.rs:311); check.rs's no-ops (`let _ = json_escape("")` in run(), `let _ = index` in agreement_pass); `Index.noun_animate` + its false doc comment (check.rs:60 — built, stored, never read since the V14.2 enrichment moved to the maps); morph.rs's unreachable `"ovati"/"irovati"/"izovati"` suffix entries (morph.rs:391); `corpus::debase_stress` (corpus.rs:373 — duplicates the `proto::debase_vowel` table made pub(crate) precisely so this couldn't drift); `derive.rs` one-line wrappers `inverse_palatalization`/`inverse_iotation` (derive.rs:135); `generator::Generation::top()` (generator.rs:25); `calibrate::CORPUS_COVERAGE_SCORE_DOMAIN` (calibrate.rs:144, superseded by v2); the official.rs TSV branch + `looks_like_tsv` (official.rs:230, no caller — quote-blind splitter beside the real parser); `glossxref::token_count` if grep still shows zero callers. Also the on-disk litter: `tools/__pycache__/`, and move `IMPROVEMENT_REPORT_V7.md`/`V8.md` to docs/history beside their prompts. Every deletion of a `pub` item checks tools/ and tests first. Acceptance: export hash unchanged, tests green minus any test that only tested deleted code (delete those with it, named in the commit).

## 2. Track B: cache-load contract enforcement (corrupt-cache paths only)

dump.rs:84-87 documents the rule the code then violates three times: "Callers must NOT collapse that error to None (`.ok()` / `.unwrap_or_default()`), or a stale cache silently degrades the output — which is exactly what the schema stamps exist to prevent." Violations: main.rs:387 (extract-enrich's raw-lemma union), coincheck.rs:292-293 (both evidence caches), falsefriends.rs:990-995 (all three caches — and falsefriends feeds the SHIPPED notes shards, the worst of the three). Route all through `dump::load_optional`: absent stays Ok(None), corrupt/stale-schema becomes a hard error. Healthy-cache behavior (the frozen baseline) is identical — state that in the commit and prove it with the export hash. Also extract the copy-pasted loader boilerplate while there: one generic `load_stamped_cache<T>` (dump.rs:232, dump.rs:1546, enrich.rs:111 are the three copies).

## 3. Close the calibrator lifecycle hole; make novel-words single-owner

The critic found the one committed artifact with no freshness guard: `data/corpus-calibration.json` (written by `corpus-eval --fit`, read by export at site/mod.rs:469) — CI git-diff-guards score-calibration.json and novel-words.tsv but not it; it can silently go stale against `corpus::build_sets` changes. Add the guard (cheapest correct form: CI runs `corpus-eval --fit` and `git diff --exit-code -- data/corpus-calibration.json`, mirroring the existing pattern — if fit runtime is prohibitive, a `--check` mode that refits and compares without writing).

`data/novel-words.tsv` has one writer (export, site/mod.rs:1622 — via a cwd-relative path into the repo mid-run) and three hand-rolled readers with duplicated column literals. Give it one owner: a `NovelWordRow { write / parse }` pair + `DEFAULT_NOVEL_WORDS` const; export builds rows in memory, writes once, and passes the rows to `check::build_index` directly (killing the same-run disk round-trip at mod.rs:1622→2032); check-text/coin-check CLI paths keep reading the committed file through the same parser. Byte-identical file output required.

## 4. One gloss tokenizer module: `src/gloss.rs`

Five-plus parallel English gloss tokenizers with different semantics, three of them crossing module boundaries: `site::english_api::{gloss_keys, english_gloss_tokens}` (which check.rs reaches into — the audit's most-cited boundary violation, flagged independently by four auditors), `dump::gloss_tokens` (the de-facto shared one — ~14 consumers including proto_link production scoring), `falsefriends::gloss_tokens`+`ordered_tokens` (reached by site/coverage.rs:510-678), `glossxref::head_tokens`. Move ALL of them **verbatim** into `src/gloss.rs` with `pub use` re-exports at their old paths — pure code motion, no unification of semantics in this PR (they differ deliberately in places; unifying is V16 material once they're side by side). This one commit removes the check→site, thesaurus→dump, and site→falsefriends reaches. Export hash unchanged.

## 5. One fold, one accent predicate, one POS-tag grammar, no hidden cache reads

- `orthography::fold_key(s)` (= `to_standard(&s.to_lowercase())`): the idiom appears **59 times** crate-wide; substitute mechanically in 2-3 batches with the CI hash between. Same commit-family: `is_combining_mark(c)` for the `'\u{0300}'..='\u{036F}'` predicate hand-inlined **10 times** (flavorize.rs:92, dump.rs:449/452, enrich.rs:183, proto.rs:120, site/search.rs:275, +4); and kill the double/triple-folds the audit found (aspect.rs:97 wraps `consonant_key` in the fold it already performs; site/model.rs:267 `ascii_skeleton(&to_standard(&lower))`).
- `src/postag.rs` (or a model.rs section): ONE grammar for the official `pos_raw` column, currently parsed by four independent substring parsers — `Pos::parse` + `parse_noun_traits` (model.rs), `aspect::aspect` (aspect.rs:31), `check::valence_char` (check.rs:248), plus eval.rs:1909's inline re-implementation of aspect's filtering. Point all five at it; `pub use` shims where signatures are public.
- `ProtoIndex::load` (dump.rs:1577) silently reads a SECOND cache (`DEFAULT_LEMMA_CACHE`, cwd-relative, ignoring the caller's path argument) — the audit's worst nonlinear edge. Make it `load_with_lemmas(proto_path, lemma_source)` with every call site passing the dependency explicitly.
- `FormRecord.lemma_key`: store the folded lemma once at sink-add (forms.rs:415) — retires the per-cell re-folds (~5% of index build, measured), the token_grammar per-record allocations, and the enrichment's residual fold, all deferred through three review rounds. Byte-identity guard: the stored key must equal what consumers computed before.

## 6. Eval machinery out of the production modules

The audit found answer-reading diagnostic code living inside production files: `consensus::Oracle`/forced-cluster/oracle-rep machinery (consensus.rs:284) and the rep-rule tables kept alive only by retired rungs (consensus.rs:270); derive.rs's 300-line benchmark writer reaching into eval+calibrate (derive.rs:436); `corpus::generate_set` vs `pipeline::generate_oracle` as drifted post-processing twins (corpus.rs:531); `aspect.rs` (leaf module) reaching UP into `pipeline::generate` (aspect.rs:183). Moves: oracle/diagnostic types and the benchmark writers relocate to eval-side modules, the twins collapse to one parameterized post-process. For generate_set ignoring its ConsensusConfig gates (corpus.rs:519): this is a Track B INVESTIGATION, not a freeze call — determine whether the shipped site's behavior matches the benchmark-validated production() config. If they agree, make the parameter honest (drop or wire it; Track A). If they DIVERGE, the ship diverges from what the benchmarks blessed — a benchmark-honesty defect: fix toward the validated config as a declared Track B commit with the export delta enumerated by the item-8 differ and the affected benchmark numbers re-measured and stated plainly. Also: library `println!` discipline — 12 files print from lib paths (build_sets corpus.rs:215 is the worst); route stats through returned structs, printing stays in main.rs/report writers. Also dedupe the ~8-copy eval harness preamble (eval.rs:791) and the 12-copy `pct` closure (eval.rs:875).

## 7. Strict lints, adopted with the census numbers

Land the census's `[workspace.lints.clippy]` table: `todo`/`dbg_macro` **deny** (zero violations today); `unwrap_used`/`unwrap_in_result`/`panic` **warn now, deny by the end of this PR** after the burn-down — lib sites: 17 unwraps across 7 files, 3 unwrap-in-result (check.rs), 1 panic (eval.rs:122's fail-fast, which becomes `#[allow]`-with-rationale or a proper error); `expect_used` stays **allow** as the sanctioned escape hatch (expect-with-message is the target state of the unwrap burn-down); `indexing_slicing` stays **allow** globally (279 lib sites) but the audit-flagged panic-risks are fixed now (thesaurus.rs:128 bracket-indexing in build; eval.rs:1207 `cands.first().unwrap()` on a defensibly-empty list; entries.rs:239 cross-function unwraps) — the full indexing burn-down is tracked per-module follow-up, not promised. Pedantic picks at **warn**: `cast_possible_truncation`, `cast_sign_loss`, `match_same_arms`, `map_unwrap_or`, `redundant_closure_for_method_calls`, `uninlined_format_args`, `needless_pass_by_value`, `too_many_lines`. Test modules get the blanket `#![allow]` block (retires the 302 test-side hits legitimately); the 8+ `catch_unwind` inflector sites consolidate into ONE fallible wrapper in forms.rs (absorbing inflect_eval.rs:76/160/241 and navigation.rs:990/1001 alongside forms.rs:129/290, check.rs:462, coincheck.rs:309, entries.rs:1653/1677) with the targeted allow + rationale on it alone. Run the two auto-fixable lints (`--fix`) as their own mechanical commit. Every wave hash-proven.

## 8. Pipeline made declared, provenanced, and fingerprinted (research adoptions)

- **`docs/PIPELINE.md`** — the stage DAG as it actually is (the extraction auditor's dataflow maps are the draft): each stage's command, declared inputs, owned outputs, and the rule that no stage reads another's raw inputs. Then make Make agree: file-target dependencies in the Makefile so the DAG is checked, not narrated (DVC's lesson without DVC).
- **Whole-output fingerprint + differ** (adopted from interslavic-rs): `dump-output`/`diff-output` subcommands producing a canonical keyed dump of the export's records and one FNV-1a number pinned in a unit test, with the failure message naming the exact regenerate/diff commands and the changelog obligation — turns "the hash changed" into an enumerated diff. The tree-sha proves identity; the fingerprint EXPLAINS non-identity.
- **`site/build-info.json`** provenance stamp (reproducible-builds lesson): git rev, crate pin, data_release, cache hashes — closing the one provenance gap artifacts have outside the release ritual. NOTE: this ADDS a file to the export — declared, additive, lands with its own guide/README line; later items' byte-identity acceptance compares post-item-8 baselines.
- **SOURCE_DATE_EPOCH discipline as a grep-lint** in CI: wall-clock APIs banned from all output paths (one committed time source), turning an implicit rule into an impossible regression.
- **`INTEGRATION.md`** at root: the downstream contract (check-text JSON schema 1, data-vN pinning + MANIFEST verification, the api/forms positional row schema, byform-order-is-API, the never-post-process rule) — mrzavec and interslavic-rs both learned these the hard way; write them down once.

## 9. Ops and doc-rot sweep

- **Move the 22 committed benchmark snapshots out of gitignored `/target/eval`** (a `cargo clean` deletes tracked files — the audit's most embarrassing ops wart) to `reports/`, updating the CI guards' paths and the Makefile. Declared repo-layout change; export untouched.
- CI: the regression floor reads the machine-readable metric the run already writes instead of grep/awk on stdout (ci.yml:97); toolchain preamble deduped (composite action or `actions-rust-lang/setup-rust-toolchain` reading rust-toolchain.toml — kills the 4× pinned 1.93.1).
- README truth pass, each with the audit's line refs: claims the site is unpublished while pages.yml deploys it; documents interslavic 0.10.0 against the =0.13.0 pin; contradicts itself on proposals.html; architecture map omits six real modules (aspect, coincheck, glossxref, release, site/english_api, site/tests). The hardcoded accuracy figures in special.rs:992+ (metrics/about pages) become values read from the committed benchmark reports at export time, so they can never rot again — NOTE: this changes page bytes; declared, and it lands beside item 9's report relocation so both page-content changes are one reviewed event.
- Makefile: cover the eval probes it omits (or declare them internal in PIPELINE.md); `clean` learns about eval scratch; dump.rs:647's prefilter builds its markers from OLD_STAGE_LANGS instead of hand-duplicated literals.

## 10. Track B: the audit's known behavior bugs, fixed not frozen

Each its own commit, wrong-before/right-after pinned:

- **Delete the broken fallback export mode** (site::export, mod.rs:73; reached only when the lemma cache is absent, main.rs:370): it emits forms.html/text-check.html whose JS requires `api/` artifacts the mode never writes — a site that 404s on its own pages is worse than an error. `export` without the cache now fails with a message naming the extract command. Removes ~240 lines plus the fallback-only renderer cluster (entries.rs:1396-1833, search.rs home_page, special.rs about_page) the audit flagged as fallback-only — verify reachability before deleting each.
- **`load_curation_notes` stops swallowing errors** (navigation.rs:429): a malformed data/curation-notes.json currently drops every curation note site-wide, silently. Absent file stays fine; unreadable/unparseable becomes a hard export error.
- **Fix the two audit-flagged panic paths** now rather than under item 7's lint umbrella if sequencing separates them: thesaurus.rs:128, eval.rs:1207.
- Anything item 6's generate_set investigation finds divergent (see item 6) lands here with its measured deltas.

## Validation (do all, report numbers)

1. Per-commit: Track A commits prove the tree hash unchanged; Track B commits (items 2, 10, parts of 6/9) prove the tree diff touches exactly the declared files and carry their before/after evidence; `cargo test` green; clippy clean at the lint levels then in force (levels ratchet within the PR).
2. End state: all 233+ tests, probe 147/44/28, evaluate 42.02% (floor untouched), aspect manifest byte-identical, `data-manifest` OK, the corpus-calibration freshness guard green, the new fingerprint test pinned.
3. `cargo clippy` with the final table: zero warnings in lib paths for every deny/warn lint except the tracked `indexing_slicing` remainder (report its end count against the census's 279).
4. PR body maps every audit finding to fixed / deferred-to-V16 / rejected-with-reason, and lists every Track B behavior change with its evidence — an undeclared behavior change found in review is a failed PR, a declared one is the point.
5. If item 6's investigation moves any benchmark number, the movement is stated with the cause in the PR body and the changelog — benchmark honesty cuts both ways: frozen-but-wrong numbers are as dishonest as silently moved ones.

## House rules (unchanged)

Deterministic and offline; benchmark honesty (nothing in this brief touches an accuracy path — that is the point); static-only; contract discipline (INTEGRATION.md, PIPELINE.md, build-info documented where consumers look, same commit as the change); reviewable commits, one per item, mechanical batches hash-proven individually; honest regressions stated plainly.
